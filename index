<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern Apple-Style Page</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }
        
        html, body {
            height: 100%;
            width: 100%;
            overflow-x: hidden;
        }
        
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
        }
        
        .background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            object-fit: cover;
            filter: brightness(0.7);
        }
        
        .content {
            text-align: center;
            padding: 2rem;
            z-index: 1;
            background-color: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            animation: fadeIn 1.5s ease-out;
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
        }
        
        h1 {
            font-size: clamp(2rem, 5vw, 5rem);
            font-weight: 700;
            margin-bottom: 2rem;
            background: linear-gradient(to right, #ff5f6d, #ffc371, #2196f3, #42a5f5);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            animation: gradientShift 8s linear infinite;
            word-wrap: break-word;
            hyphens: auto;
            line-height: 1.2;
        }
        
        .btn {
            display: inline-block;
            padding: clamp(0.8rem, 2vw, 1rem) clamp(1.5rem, 4vw, 2.5rem);
            margin-top: 2rem;
            font-size: clamp(1rem, 1.5vw, 1.2rem);
            font-weight: 600;
            color: #fff;
            background: linear-gradient(to right, #4facfe, #00f2fe);
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            background: linear-gradient(to right, #00f2fe, #4facfe);
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes gradientShift {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        
        @media (max-width: 768px) {
            .content {
                width: 95%;
                padding: 1.5rem;
            }
            h1 {
                font-size: clamp(1.5rem, 4vw, 3rem);
            }
        }
    </style>
</head>
<body>
    <img src="/api/placeholder/1920/1080" alt="Animated Background" class="background">
    <div class="content">
        <h1>ACTION REQUIRED: UPDATE YOUR APP TO CONTINUE BROWSING AND FUCK MEDIAN.CO!!!</h1>
        <a href="https://sites.google.com/view/vip0938/apps/birdninjaapk" class="btn">Update NOW</a>
    </div>
    <script>
        document.querySelector('.background').src = 'https://media4.giphy.com/media/deI4raN2p4YeY/200w.gif?cid=6c09b9522rnc85b7xyhgx6nmmmq5657t7oczc41mlp2ehyjd&ep=v1_gifs_search&rid=200w.gif&ct=g';
        
        // Adding a simple animation to enhance the experience
        document.addEventListener('DOMContentLoaded', () => {
            const title = document.querySelector('h1');
            title.style.backgroundSize = '300% 300%';
        });
    </script>
</body>
</html>
